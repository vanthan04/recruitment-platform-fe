BACKEND_URL=http://localhost:8080

# Backend origin exposed to the browser, for the realtime chat socket to
# connect directly to (no edge proxy in front — see src/lib/realtime/socket.ts).
# Required in production too, set to the backend's real public origin.
NEXT_PUBLIC_BACKEND_URL=http://localhost:8080
