BACKEND_URL=http://localhost:8080

# Dev-only escape hatch for the realtime chat socket — leave unset in
# production. See src/lib/realtime/socket.ts and
# https://github.com/vanthan04/recruitment-platform-edge (separate repo —
# the Cloudflare Worker that makes the relative-path connection work in
# production).
NEXT_PUBLIC_BACKEND_URL=http://localhost:8080
